# 밑빠진 독상 (dokseong)

함께하는 시민행동 **밑빠진 독상** 사이트. 2000년 제1회부터 2025년 제40회까지 이어온
예산낭비 감시 활동을, 제41회부터는 **분기마다** 시민 제보와 투표로 함께 만들어갑니다.

> **★ 이 사이트는 별도 페이지로 계속 갑니다 (2026-09-03 사용자 결정).**
> 캠페이너스 홈페이지 안으로 **이식하지 않습니다.** 예전 문서 곳곳에 "나중에
> 통째로 붙여넣을 예정"이라고 적혀 있었는데, 그 전제는 폐기됐습니다.
> 되묻지 마세요.

> **운영 구조 변경(2026-08-24):** 실제 제보는 캠페이너스 입력폼, 서명은
> 도너스, 투표는 12월에 다시 정합니다. 이 저장소의 D1/R2/API 구현은 과거
> 설계이며 새 운영에는 사용하지 않습니다. 현재 상태와 남은 작업은
> [OPERATIONS.md](OPERATIONS.md)를 기준으로 확인하세요.

```
제보 접수 → 후보 선정 → 시민 투표 → 수상 발표 → 캠페인 서명 → (다음 분기)
```

## 구조

```
dokseong/
├── public/
│   ├── index.html          # 페이지 전체 (HTML + CSS + JS 한 파일)
│   ├── privacy.html        # 개인정보 처리방침 (시민행동이 실제로 쓰는 방침 전문)
│   └── data/
│       ├── map-sido.json       # 시도 16개 (SVG 경로, 전남광주 통합 반영)
│       ├── map-sigungu/        # 기초자치단체 229개 (아직 화면에 안 씀)
│       ├── awards.json         # 역대 수상 40건 + 지역 코드
│       └── cases.json          # 월간 좋은예산 사례 10건 (요약은 검수 전 초안)
├── functions/              # ⚠️ 옛 설계. 새 운영에서 쓰지 않습니다 (OPERATIONS.md)
│   └── api/
│       ├── report.js       # POST /api/report  — 시민 제보 접수
│       └── regions.js      # GET  /api/regions — 지역별 제보 건수
├── tools/                  # 데이터 만드는 스크립트 + 굽는 스크립트 (아래 참고)
├── docs/goodbudget/        # 좋은예산센터 원문 백업 (이식이 아니라 백업입니다)
├── schema.sql              # D1 테이블 정의 (옛 설계)
└── wrangler.toml           # Cloudflare Pages 설정 · 회차 번호
```

**Cloudflare Pages + Functions**를 씁니다. 빌드 단계가 없습니다 —
`public/`은 그대로 서비스되고, `functions/` 안의 파일 하나가 그대로 API 주소가 됩니다
(`functions/api/report.js` → `/api/report`).

> 예산게임(budget-mbti)의 Vite + Worker 구성은 일부러 따라가지 않았습니다.
> 로컬에서 DB 붙이는 것만도 손이 많이 가는 구조라, 여기서는 단순한 쪽을 골랐습니다.

## 별도 페이지로 갑니다 (2026-09-03 결정)

캠페이너스 안으로 옮기지 않고, 이 저장소가 배포하는 **독립된 사이트로 계속** 운영합니다.
그래서 아래 것들이 **임시가 아니라 최종 형태**입니다.

| | 이식을 전제했을 때 | 별도 페이지로 갈 때 (지금) |
|---|---|---|
| 바깥 파일 읽기 (`data/*.json`) | 상대주소가 404 나서 절대주소가 필요 | **상대주소 그대로** 써도 됩니다 |
| 제보 접수 | 이식하면 캠페이너스 입력폼 위젯을 그 자리에 심는다 | **새 탭으로 여는 링크가 끝까지 갑니다** |
| 우리 제보 팝업 | 이식 전까지만 쓰는 임시 화면 | **접수처가 바깥이면 쓰이지 않습니다** — 아래 참고 |
| `#dok` CSS 가두기 | 홈페이지를 안 깨뜨리려고 | 이유는 없어졌지만 **그대로 지킵니다** |

⚠️ **정하지 않은 것 하나** — 접수를 캠페이너스 입력폼(`mode: 'link'`)으로 넘기면
`#dok-report-form-modal` 의 우리 제보 폼은 화면에서 안 쓰이게 됩니다.
사진 미리보기·자동 축소·지역 자동 채우기가 거기 붙어 있습니다.
**쓰지 않을 화면을 남겨 두면 다음 사람이 되는 기능인지 아닌지 알 수 없게 됩니다.**
접수처를 붙이는 날 지울지 남길지 정하세요.

## 굽는 스크립트 — 결과 파일을 손으로 고치지 마세요 (중요)

`public/index.html` 의 일부는 **다른 파일에서 구워 낸 것**입니다. 손으로 고치면
다음에 구울 때 사라집니다.

| 스크립트 | 원본 | 덮어쓰는 곳 |
|---|---|---|
| `tools/port-summary.py` | `../action-home-renewal/dok-history.html` | `index.html` 의 회차 요약 블록 |
| `tools/port-footer.py` | `../action-home-renewal/tools/parts/footer.html` | `index.html` 맨 아래(후원 띠 + 푸터) |
| `tools/fetch-goodbudget.py` | goodbudget.kr | `docs/goodbudget/` |

⚠️ **`port-summary.py` 는 표시 사이를 통째로 다시 만듭니다.** 그 안에 코드를 더하면
지워집니다(2026-09-02 에 사례 연결을 한 번 지워 먹었습니다). 사례를 다루는 코드는
블록 **밖** 별도 `<script>` 에 두었고, 생성기는 `window.dokModal` 내보내기만 만듭니다.

⚠️ **회차 요약 원본이 두 곳입니다** — 개편 저장소의 `dok-history.html` 과 여기.
사용자 결정(A)이라 그대로 갑니다. 요약을 고칠 때는 둘 다 고치거나
`port-summary.py` 를 다시 돌리세요.

## CSS 범위 규칙 (중요)

모든 CSS 규칙이 `#dok` 안에 갇혀 있고, 클래스는 전부 `dok-` 로 시작합니다.

- ❌ `.card { ... }`, `body { ... }`, `* { ... }`
- ✅ `#dok .dok-card { ... }`

> **이 규칙은 원래 캠페이너스 이식을 위한 것이었습니다.**
> 2026-09-03 에 **이식하지 않기로** 정했으므로 그 이유는 사라졌습니다.
> 그래도 규칙은 **그대로 지킵니다** — 이미 4,000줄이 이 방식으로 쓰여 있고,
> 지금 와서 푸는 것은 얻는 것 없이 위험만 큽니다. 이 페이지에 새 코드를 더할 때도
> 같은 방식으로 쓰세요.

`public/index.html` 안의 주석에 남아 있는 `캠페이너스 이식 단위` 표시도
같은 이유로 지우지 않았습니다. **읽을 때 이식 계획으로 오해하지 마세요.**

## 예산감시 사업 내용 고치는 법

분기마다 손대는 곳은 `public/index.html` 안 두 군데입니다. **코드는 건드리지 않습니다.**

1. **목록** — `<script type="application/json" id="dok-campaign-data">`
   카드에 보이는 제목·요약·기간·상태(`ongoing`/`closed`)·결과 한 줄.
2. **상세 내용** — `<div id="dok-camp-bodies">` 안의 `<article data-camp="아이디">`
   카드를 눌렀을 때 열리는 보고서 본문. 그냥 HTML이라 `<h4>`, `<p>`, `<a>`를
   평소처럼 쓰면 됩니다. JSON 안에 긴 글을 넣으면 줄바꿈·따옴표 때문에
   쓰기 어려워서 일부러 나눴습니다.

두 곳의 **`id`와 `data-camp` 값이 같아야** 연결됩니다.
`<article>`이 없으면 "아직 정리된 내용이 없습니다"가 뜨고, 오류가 나지는 않습니다.

- 진행 중인 사업 → 무엇이 문제인지, 지금까지 확인한 것, 앞으로 할 일
- 종료된 사업 → 무엇이 문제였는지, 무엇이 남았는지 (+ `outcome` 한 줄이 강조돼 표시됨)

## 이야기 채우는 법

히스토리 칸에는 원래 역대 수상 4건이 카드로 있었지만, 같은 목록이 **지도**와
**전체 수상 표**에 이미 나와서 뺐습니다. 그 자리는 상을 만들어 온 사람들의
글로 씁니다. 활동가 연재 글을 먼저 넣고, 후원자·회원 이야기는 나중에 붙입니다.

**글이 한 편도 없으면 이 칸은 화면에 나오지 않습니다.** 현재는 채연하 활동가의
첫 글 한 편이 들어가 있습니다.

고칠 곳은 `public/index.html` 안 두 군데입니다. **코드는 건드리지 않습니다.**

### 1. 목록 — `<script type="application/json" id="dok-story-data">`

```json
[
  {
    "id": "2026-08-yeongwol",
    "title": "영월에 다녀왔습니다",
    "author": "홍길동",
    "role": "함께하는 시민행동 예산감시팀",
    "date": "2026. 8",
    "excerpt": "카드에 보이는 두세 줄. 전문에서 그대로 따와도 됩니다.",
    "link": ""
  }
]
```

**적은 순서대로 나옵니다.** 새 글은 맨 위에 넣으세요.
`role`·`link`는 비워도 됩니다.

### 2. 본문 — `<div id="dok-story-bodies">` 안

```html
<article data-story="2026-08-yeongwol">
  <p>첫 문단.</p>
  <p>둘째 문단. <a href="...">링크</a>도 됩니다.</p>
</article>
```

목록의 `id`와 `data-story` 값이 **같아야** 이어집니다.

| 넣은 것 | 카드에 생기는 것 |
|---|---|
| 본문 `<article>` | **이어서 읽기** — 카드 안에서 펼쳐집니다 |
| 본문 없이 `link`만 | **원문 보기** — 새 창으로 열립니다 |
| 둘 다 없음 | 발췌까지만 |

### 소제목 문구

카드 위 두 줄("상을 주는 사람들" / "밑빠진 독상을 만들어 온 활동가들의 글입니다")은
`index.html`에 그냥 적혀 있습니다. 마음에 안 들면 그대로 고쳐 쓰면 됩니다.

### 후원자·회원 이야기를 붙일 때

같은 목록에 그냥 추가하면 활동가 글과 같은 카드로 나옵니다. 글이 짧아서
카드 크기를 달리하고 싶다면, 항목에 `"kind": "supporter"` 같은 칸을 만들고
`.dok-story-card` 에 클래스를 하나 더 붙이는 정도로 충분합니다.
**미리 만들어 두지는 않았습니다** — 쓰지 않는 화면에 기능이 붙어 있으면
나중에 이게 되는 기능인지 아닌지 알 수 없게 되기 때문입니다.

## 지도

시도 단위까지만 그립니다. 지도를 누르거나, 아래 **지역 칩**을 눌러 지역을 고릅니다.
칩이 따로 있는 이유는 모바일(334px)에서 인천·대전·세종 같은 작은 시도를
손가락으로 누를 수 없기 때문입니다. 지도는 보는 것, 칩은 누르는 것입니다.

기초자치단체(시군구) 드릴다운은 데이터가 이미 `public/data/map-sigungu/`에
준비돼 있고, 행정구역 변경분을 반영한 뒤에 붙일 예정입니다.

### 전남광주통합특별시

광주광역시와 전라남도가 통합되어, 지도에서 두 지역을 **하나로 합쳤습니다.**

- 코드는 전남의 `36`을 그대로 씁니다. 이미 D1에 쌓인 제보의 `sido` 값과
  API의 두 자리 검사(`/^\d{2}$/`)를 그대로 통과하기 때문입니다.
- 광주 코드 `24`는 지도에서 사라졌습니다. **광주로 접수된 제보가 있다면**
  아래 SQL로 옮겨야 지도 집계에 잡힙니다.

```bash
npx wrangler d1 execute dokseong --remote \
  --file=migrations/2026-08-24-gwangju-to-jeonnam.sql
```

- 전남 도형 안에 광주 자리가 구멍으로 뚫려 있던 것을 **구멍째 지웠습니다.**
  광주 경로를 덧대는 방법도 있지만, 그러면 합쳐 놓고도 경계선이 그대로 남습니다.
- `public/data/map-sigungu/24.json`(광주 5개 자치구)과 `36.json`은 **아직 그대로**입니다.
  시군구 드릴다운을 붙일 때 두 파일을 합쳐야 합니다. 그때 `report.js`의
  `data.sido = data.sigungu.slice(0, 2)`도 손봐야 합니다 — 광주 시군구 코드는
  `24`로 시작해서, 그대로 두면 없어진 시도 코드가 다시 생깁니다.

> 지도 원본(통계청 2018)에는 이후 바뀐 이름들이 반영돼 있지 않습니다.
> 강원도·전라북도 등이 화면에 옛 이름으로 나옵니다. 확인되면 함께 고쳐 주세요.

지도 데이터를 다시 만드는 방법은 [tools/README.md](tools/README.md)를 보세요.

## 회차 관리

분기마다 바뀌는 값은 코드가 아니라 `wrangler.toml`의 `[vars]`에 있습니다.

| 변수 | 뜻 |
|---|---|
| `CURRENT_ROUND` | 지금 진행 중인 회차 (제41회 = `"41"`) |
| `REPORT_OPEN` | `"0"`으로 두면 제보 접수가 닫힘 |
| `REPORT_DEADLINE` | 이번 차수 마감일. 제보는 상시 받되 이 날까지를 한 묶음으로 봄 |
| `REPORT_DEADLINE_LABEL` | 화면에 뜨는 이름 (예: 하반기 1차 제보 마감) |

> **마감 안내는 2026-09-02 에 화면에서 뺐습니다**(사용자 지시 — 실제로는 상시 접수라
> 마감이 없습니다). `#dok-deadline` 은 이제 없고, `wrangler.toml` 의 `REPORT_DEADLINE`
> 값만 남아 있습니다. 되살리려면 그 커밋 이전 판을 보세요.

## 처음 한 번 해야 하는 세팅 (옛 설계 — 지금은 안 합니다)

> 2026-08-24 운영 구조 변경으로 **D1·R2·`schema.sql` 은 쓰지 않습니다**
> (`OPERATIONS.md`). 아래는 비교·롤백용 기록입니다. 새로 세팅하지 마세요.

```bash
# 1. D1 데이터베이스 만들기
npx wrangler d1 create dokseong
#    → 출력된 database_id를 wrangler.toml에 붙여넣기

# 2. 테이블 만들기
npx wrangler d1 execute dokseong --remote --file=schema.sql

# 3. IP 해시용 소금값 (제보자 IP를 원본으로 저장하지 않기 위함)
npx wrangler pages secret put IP_SALT
```

## 로컬에서 보기

```bash
python tools/preview.py
```

→ http://localhost:8321/

⚠️ 인자를 줄 거면 **둘 다** 주세요 — `python tools/preview.py public 8321`.
포트만 주면 그 숫자를 폴더 이름으로 읽어 404 가 납니다.

파일을 고치면 **브라우저가 알아서 새로고침**합니다. 캐시도 꺼져 있어서
"고쳤는데 화면이 그대로"인 일이 없습니다.
(자동 새로고침 스크립트는 서버가 내려줄 때 끼워 넣는 것이라 배포본에는 안 들어갑니다.)

DB가 붙은 화면까지 보려면:

```bash
npx wrangler d1 execute dokseong --local --file=schema.sql   # 최초 1회
npx wrangler pages dev public --d1 DB=dokseong
```

`tools/preview.py` 로 볼 때는 DB가 없으므로 제보·참여를 제출하면
"접수 중 문제가 생겼습니다"가 뜹니다. **정상입니다.**

## 공개 안전장치 (2026-09-02)

이 사이트는 이미 밖에서 열립니다(`dokseong-action.bada523082.chatgpt.site`).
그래서 **확인 안 된 값**과 **갈 곳 없는 개인정보 입력**을 화면에 두면 안 됩니다.
`public/index.html` 스크립트 맨 위에 스위치 두 개를 뒀습니다. **파일을 뜯어고치지 말고
그 두 곳만 바꾸세요.**

### `DOK_SHOW_SAMPLE` (지금 `false`)

예시(더미) 내용을 화면에 낼지 정합니다. 실제 값으로 갈아 끼운 뒤 `true` 로 바꾸세요.
`false` 인 지금 감춰지는 것 —

| 감춘 것 | 어디 |
|---|---|
| 예산감시 사업 4건(전부 `(예시)`)과 상세 보고서 | `#dok-campaign-data` |
| 제41회 투표 후보 5건 | `#dok-vote-fold` 통째 |
| '이렇게 제보가 쓰입니다' 예시 제보 2건 | `#dok-sample-data` |
| 첫 화면 지표 **1,000억 원** | `.dok-stat-item.is-lead` |

`40회`·`26년` 두 지표는 실제 값이라 그대로 둡니다.
사업 목록 자리에는 '정리하고 있습니다' 안내가 뜹니다(빈 화면이 되지 않습니다).

### `DOK_INTAKE` (지금 셋 다 `disabled`)

폼이 받은 것을 어디로 보낼지 정합니다.

- `'disabled'` — **보내지 않습니다.** 칸과 버튼이 잠기고 안내만 뜹니다.
- `'link'` — 우리 폼 대신 `url` 의 바깥 접수처로 보냅니다(캠페이너스 입력폼).
- `'donus'` — **서명 전용.** 우리 참여 팝업에서 도너스 Forms API 로 바로 씁니다.

```js
const DOK_INTAKE = {
  report:  { mode: 'disabled', url: '' },   // 예산낭비 제보 → 캠페이너스 입력폼
  join:    { mode: 'disabled', url: '' },   // 감시 참여·서명 → 도너스
  regions: { mode: 'disabled', url: '' },   // 지역별 제보 건수 집계
};
```

**빗장을 둘 두었습니다.** 화면에서 칸을 잠그는 것 하나, 제출 처리 맨 앞에서 되돌리는 것
하나입니다. 개발자도구로 `disabled` 를 풀어도 이름·이메일·전화번호·제보 사진이
바깥으로 나가지 않습니다.

### 서명을 여는 법 (도너스, 2026-09-03)

`index.html` 의 `DOK_INTAKE.join.mode` 를 `'donus'` 로 바꾸고, 바로 아래 `DOK_DONUS`
에서 **`formkey` 와 `channel` 두 칸**을 채우면 열립니다. **둘 중 하나라도 비어 있으면
`'donus'` 라고 적어도 폼은 잠긴 채로 있습니다** — 빈 값으로 켜서 개인정보가 엉뚱한
데로 나가는 일을 막으려는 것입니다(`dokJoinReady()`).

| `DOK_DONUS` 칸 | 도너스 어디에서 | 필수 |
|---|---|---|
| `formkey` | 관리시스템에서 발급한 Formkey | 필수 |
| `channel` | 기초정보 > 활동채널 → 활동채널코드 | 필수 |
| `category` | 기초정보 > 활동분류 → 활동분류코드 | 선택 |
| `consentField` | 활동기록 사용자정의필드 `info1`~`info5` 중 하나 | 선택 |

보내는 것은 `POST https://cloud.donus.org/api/forms/v0/submit/interact/<formkey>` 하나이고,
`registerMember: true` 로 이메일(없으면 휴대폰)로 기존 구성원을 찾아 붙입니다.
활동유형은 `ENG`(참여), 활동명은 `[밑빠진 독상] <사업 제목>`, 태그는 `밑빠진 독상` + 사업 id,
UTM 5종은 도너스 이름(`utmSource` 등)으로 옮겨 담습니다.

- **CORS 는 확인했습니다** (2026-09-03). 도너스가 `access-control-allow-origin: *` 을
  주므로 어느 도메인에서 올려도 브라우저가 바로 POST 할 수 있습니다.
- **`updateOnMatch` 는 `false` 입니다.** 이미 도너스에 있는 사람이 서명해도 그 사람의
  이름·수신동의를 이 폼 값으로 덮어쓰지 않습니다. 공개된 폼에서 들어온 값이 기존
  후원회원 기록을 덮으면 되돌리기 어렵습니다(활동기록은 정상적으로 남습니다).

  **2026-09-03 에 실제로 시험해서 확인했습니다.** 도너스 도움말에는 "이메일 또는
  휴대전화번호가 중복되는 구성원이 있으면 해당 구성원의 수신동의 값이 변경됩니다"
  라고 적혀 있어 위험해 보였는데, `updateOnMatch:false` 면 수신동의도 안 바뀝니다.
  시험 방법 — 시험 구성원을 한 건 넣고(전화·우편 자동으로 '거부'), 사람이 그 둘을
  '동의' 로 바꾼 뒤, 같은 이메일로 한 번 더 제출했더니 **'동의' 가 그대로 남았습니다.**
  활동기록만 2건으로 쌓였습니다. `updateOnMatch` 를 `true` 로 바꾸면 이 보호가
  사라지므로, 바꾸기 전에 같은 시험을 다시 하세요.
- **전화·우편 수신동의는 명시적으로 `false` 를 보냅니다.** 도너스는 이 칸들을 안 보내면
  전부 `true` 로 둡니다. 폼에 "광고 전화는 하지 않습니다" 라고 적어 놓고 동의로
  넣으면 거짓말이 됩니다.
- ⚠️ **참여자 수(`✊ N명이 함께하고 있습니다`)는 도너스에서 못 읽습니다.** Forms API 는
  쓰기 전용이고, 집계를 읽으려면 API Key 가 필요한데 그 키는 브라우저에 두면 안 됩니다.
  그래서 `'donus'` 에서는 집계를 부르지 않고 숫자 줄을 비웁니다(0 명처럼 보이지 않게).
  숫자를 다시 띄우려면 어디선가 집계본을 받아 `joinCounts` 에 넣어 주면 됩니다.

**하드코딩돼 있던 `/api/report` · `/api/join` · `/api/regions` 를 모두 걷어냈습니다.**
2026-08-24 운영 구조 변경(`OPERATIONS.md`)으로 그 주소들은 쓰지 않습니다 — 부르면
실패하거나 아무 데도 저장되지 않았습니다. 이제 주소는 `DOK_INTAKE` 한 곳에만 있습니다.

### 접수처를 붙이려면 받아야 하는 값

`OPERATIONS.md` 2·3장에 정리돼 있습니다.

- **제보** — 캠페이너스 입력폼 주소. 사진은 `사진 1~3` 세 항목(전부 선택)으로.
- **서명** — 도너스 **Formkey · 활동채널코드** 둘만 있으면 열립니다(활동분류코드·
  사용자정의필드는 선택). 넣는 자리와 나머지 규칙은 위 [서명을 여는 법](#서명을-여는-법-도너스-2026-09-03).
  ⚠️ 일반 API Key 를 브라우저에 넣지 말 것. Forms API 의 Formkey 만 씁니다 —
  API Key 하나면 후원자 명부 전체가 조회·수정됩니다.

### 후원으로 보내는 곳 — `DOK_DONATE` (2026-09-03)

주소를 **한 곳에서** 정합니다. 따라다니는 후원 바와 **서명 완료 화면**이 같이 씁니다.

```js
const DOK_DONATE = {
  url:   '',                  // 도너스 온라인모금 > 모금함 주소. 비면 action.or.kr
  label: '후원으로 힘 보태기',  // 금액을 정한 모금함이면 '1,000원으로 힘 보태기' 처럼
};
```

**서명을 마친 화면에 후원 권유를 답니다.** 서명 앞에 결제를 두지 않은 것은 의도입니다 —
문 앞에서 돈을 받으면 서명하는 사람 수가 크게 떨어지고, 밑빠진 독상은 "몇 명이
분노했는가" 를 보여주는 캠페인이라 그 숫자가 죽으면 캠페인이 약해집니다.
방금 이름을 더한 사람이 가장 마음이 열려 있는 순간이라 거기에 두었습니다.

클릭은 `donate_click` 으로 세되 `from: 'join_done' | 'bar'` 로 갈라 기록합니다.
어느 쪽이 실제로 후원으로 이어지는지 보려는 것입니다(GA4 ID 를 넣어야 쌓입니다).

### 개인정보 — 아직 사람이 확인해야 하는 것

`public/privacy.html` 은 2026-09-01 에 시민행동이 실제로 쓰는 방침 전문으로 갈았습니다.
**빈칸은 없습니다**(위 '이어서 할 일'의 "16곳 채우기"는 지난 이야기입니다).
대신 남은 어긋남이 있습니다 —

1. 그 방침은 **회원·후원자 문서**라 제보·제보 사진·서명·해외 클라우드 보관을 다루지 않습니다.
2. 별첨 **'캠페인 소식 및 후원 정보 수신 동의'** 가 빠졌습니다(폼 안에 같은 글이 따로
   있어 화면은 안 깨집니다).
3. 회신 이메일이 추첨 당첨 안내에도 쓰이는데 방침의 이용 목적에는 추첨이 없습니다.
4. 방침 2번의 제보 항목은 "필수: 사업명" 인데 실제 폼에는 사업명 칸이 없습니다
   (사진 첨부로 대체했습니다).

사본: `docs/개인정보-처리방침-확인요청.md`. **관리자 회신 전에는 `DOK_INTAKE` 를 열지 마세요.**

## 이어서 할 일

작업을 멈췄다가 다시 시작할 때 볼 곳입니다.

### ★★ 지금 막혀 있는 것 하나 — 배포 (2026-09-03)

**서명은 코드상 열려 있지만 시민은 아직 서명할 수 없습니다.** `main` 과 살아 있는
사이트가 다릅니다 — 2026-09-03 작업(도너스 연동 전부)이 배포되지 않았습니다.

- 살아 있는 주소: `dokseong-action.bada523082.chatgpt.site` (GPT 가 올린 곳)
- **`main` 을 배포해 달라고 GPT 에게 요청해야 합니다.** Claude 는 그 호스팅에
  접근할 수 없습니다(GitHub 푸시까지는 Claude 가 합니다).
- 대안으로 **GitHub Pages** 를 켜는 방법이 있습니다(저장소가 공개이고 이제 서버가
  필요 없어 정적으로 충분합니다). 주소가 `bada17.github.io/dokseong` 으로 바뀌는
  결정이라 **사용자가 "서명 열고 나서 정하겠다" 고 보류했습니다.**

배포된 뒤에는 실제 화면에서 한 번 서명해 보고, 도너스 `서비스 > 활동기록` 에
들어왔는지 확인하세요.

### ★ 공개된 화면에 그대로 나가면 곤란한 것 (먼저 정할 것)

이 셋은 **사람이 정해 줘야** 넘어갑니다. 사이트는 이미 밖에서 열립니다.

1. **후원자 소감 3장이 지어낸 예시입니다.** 실제 후원자의 말이 아니라 형식을 보여주려고
   Claude 가 쓴 문장이고, 카드마다 빨간 `예시` 딱지가 붙어 있습니다.
   → 실제 소감으로 갈아 끼우거나, `DOK_SHOW_VOICES` 를 `false` 로 끄세요.
2. **`public/data/campaigns.json` 의 본문이 자리표시 문장입니다.**
   "여기에 무엇이 문제인지 적습니다" 같은 글이 그대로 카드에 뜹니다.
   → 실제 사업 내용으로 바꾸세요.
3. **사업 사진이 한 장도 없습니다.** 카드와 팝업 위가 전부 회색 '이미지 자리'입니다.
   → 가로로 넓은 사진 한 장씩. 16:8 로 잘립니다.

### 바로 할 수 있는 것

- 활동가 연재 글 더 넣기 → 위 [이야기 채우는 법](#이야기-채우는-법)
- 제보 예시 2건을 실제로 들어온 제보 중 확인 끝난 것으로 교체
- 연재 글 딥링크 `#story-…` — **글이 한 편뿐이라 미뤘습니다.** 여러 편이 되고
  뉴스레터에서 특정 글로 보내야 할 때 만드세요. 코드 스무 줄

**값을 받아야 시작할 수 있는 것**
- **GA4 측정 ID** — 지금 `G-XXXXXXXXXX` 자리표시자입니다. 그래서
  2026-09-02 에 넣은 지도 클릭 기록(`map_select_sido` 의 `from: map|chip`)이
  **한 건도 안 쌓이고 있습니다.** "사람들이 지도보다 이름 알약을 누를 것"이라는
  가설은 ID 를 넣은 뒤부터 확인할 수 있습니다
- 캠페이너스 입력폼 주소 → `DOK_INTAKE.report` 를 `link` 로 (사진은 `사진 1~3`)
- ~~도너스 Formkey · 활동채널코드~~ → **2026-09-03 에 받아서 넣었습니다. 서명 열렸습니다.**
- **도너스 모금함 주소 → `DOK_DONATE.url`** — 서명 완료 화면의 후원 버튼이 갈 곳입니다.
  온라인모금 > 모금함 에서 만들고 URL 생성기로 뽑으세요. 지금은 비어 있어
  시민행동 홈으로 갑니다(버튼이 죽어 있지는 않습니다). 금액을 정했으면 `label` 도 함께
- 월간 좋은예산 사례 10건 **사람 검수** → 끝나면 `public/data/cases.json` 의
  `draft` 만 `false` 로 바꾸면 화면 딱지가 함께 사라집니다
- 원문을 화면에 게시해도 되는지 좋은예산센터에 확인

**남이 해줘야 하는 것**
- `dok.action.or.kr` DNS·인증서 → 시민행동 웹 담당. **지금은 TLS 에서 끊깁니다**
  (살아 있는 주소는 `dokseong-action.bada523082.chatgpt.site`). [DEPLOY.md](DEPLOY.md) 4번
- **개인정보 방침의 노란 칸 2곳** → `public/privacy.html` 6장 위탁 표의 **캠페이너스 줄**
  (법인명·보유 기간). **제보 접수를 열기 전에 채우세요.** 지금은 제보가 잠겨 있어
  그쪽으로 개인정보가 가지 않습니다.
  도너스 줄은 2026-09-03 에 채웠습니다 — (주) 크레비스파트너스 / 위탁계약 종료 시까지.
  (`docs/개인정보-처리방침-확인요청.md` 는 D1/R2 시절 전제로 쓰인 낡은 문서입니다.
   보낼 일이 생기면 새 운영 구조에 맞게 다시 쓰세요.)

**아직 손 안 댄 것**
- 시군구 드릴다운 (데이터는 `public/data/map-sigungu/` 에 준비돼 있음)
- 투표 (2026년 12월 재결정, 지금은 감춰 둠)
- 회차별 결과 아카이브

> 관리자 화면은 만들지 않습니다 — 제보는 **캠페이너스 관리자모드**에서,
> 서명은 **도너스**에서 봅니다 (`OPERATIONS.md`).

### 서명한 사람은 도너스 어디에서 보나 (2026-09-03)

**`서비스 > 활동기록`** 입니다. 구성원 목록(`모금 > 구성원`)과는 별개 화면이라
후원자 명부와 섞이지 않습니다.

- 탭 **`참여`** 를 누르면 활동유형 `ENG` 만 모입니다. 서명은 전부 `ENG` 로 보냅니다.
- **`+상세검색` → 활동채널 `밑빠진독상(미래대응기금)`(AC008)** 으로 좁히면
  밑빠진 독상 서명만 나옵니다. 사업별로 더 쪼개려면 태그(사업 id)로 거르세요.
- 오른쪽 아래 **`출력`** 으로 엑셀로 뽑습니다.

**구성원 목록에서는 갈라낼 수 없습니다.** `전체/개인/단체` 탭은 도너스가 고정한
것이고, Forms API 로는 구성원분류·그룹을 지정할 수 없습니다(쓸 수 있는 것은 이름·
이메일·휴대폰·메모·수신동의·사용자정의필드뿐). 게다가 `updateOnMatch:false` 라
**이미 있는 사람이 서명하면 구성원 정보를 아무것도 안 건드립니다** — 구성원 쪽에
"서명함" 표시를 달아도 기존 후원회원에게는 안 달려서 반쪽이 됩니다.
그래서 사람이 아니라 **행위(활동기록)** 를 기준으로 가릅니다.

섞이는 것이 손해만은 아닙니다. 서명만 한 사람이 나중에 후원회원이 되면 같은 구성원으로
이어져서 "서명하고 후원까지 간 사람" 을 셀 수 있습니다. 명부를 갈라 두면 그것을 잃습니다.

**접수처를 붙이는 날 같이 정할 것** — `DOK_INTAKE.report` 를 `link` 로 바꾸면
우리 제보 팝업(`#dok-report-form-modal`)이 화면에서 안 쓰이게 됩니다. 거기에
사진 미리보기·자동 축소·지역 자동 채우기가 붙어 있습니다. **그날 지울지 남길지
정하세요.** 쓰지 않는 화면을 그대로 두면 다음 사람이 되는 기능인지 알 수 없게 됩니다.

## 지금 상태

| 기능 | 상태 |
|---|---|
| 첫 화면 지표 | ✅ 40회 · 27년 · 들여다본 예산 **175,824억 원** — 셋 다 근거 있는 값 |
| 역대 수상 40회 | ✅ 표 · 지도 · 회차 요약 팝업 (앞뒤 넘기기 · Esc · `#dok-40` 딥링크) |
| 시도 지도 | ✅ 수상 기록 + 지역 칩 — **시도 단위까지만 (16개)** |
| 월간 좋은예산 사례 10건 | 🟡 지도에서 지역을 고르면 나옴 — **요약은 Claude 초안**, 딱지 붙어 있음 |
| 활동가 연재 글 | ✅ 채연하 활동가의 첫 글 1편 |
| 제보 칸 | 🟡 화면은 완성(큰 버튼 → 팝업) — **폼은 잠겨 있음** |
| 캠페인 참여·서명 | ✅ 도너스 연동 끝 — 코드상 열려 있음. **배포가 되어야 실제로 열립니다** |
| 따라다니는 후원 바 · 공통 푸터 | ✅ 홈페이지 공통 하단을 그대로 씀 |
| 개인정보 처리방침 | 🟡 시민행동 실제 방침 전문 — 제보·사진·서명을 안 다룸 (아래 참고) |
| 예산감시 사업 4건 · 사업 보고서 | ⬜ 예시라 `DOK_SHOW_SAMPLE=false` 로 감춰 둠 |
| 시민 투표 | ⬜ 2026년 12월 재결정, 지금은 감춰 둠 |
| 지역별 제보 건수 | ⬜ 껐음 — 옛 D1 API 를 쓰던 기능 |
| 시군구 드릴다운 | ⬜ 행정구역 변경분 반영 후 |
| GA4 | ⬜ `G-XXXXXXXXXX` 자리표시자 — **지금은 아무 숫자도 안 쌓입니다** |

> 🟡 는 "화면은 됐는데 뒤가 안 붙었다"는 뜻입니다. 제보·서명 폼이 잠긴 이유는
> 위 [`DOK_INTAKE`](#dok_intake-지금-셋-다-disabled) 를 보세요.

## 개인정보 처리방침 (`public/privacy.html`)

**2026-09-01 에 통째로 갈았습니다.** 기후솔루션 뼈대로 만들었던 초안(빈칸 16곳)은
버렸고, 지금은 **시민행동이 실제로 쓰는 방침 전문**(action.or.kr/8)이 그대로
들어가 있습니다. 그래서 **채워 넣을 빈칸은 없습니다.**

남은 어긋남은 위 [개인정보 — 아직 사람이 확인해야 하는 것](#개인정보--아직-사람이-확인해야-하는-것)에
정리했습니다. 요약하면, 그 방침은 **회원·후원자 문서**라 이 사이트가 실제로 받는
제보·제보 사진·서명·해외 클라우드 보관을 다루지 않습니다.

⚠️ 갈아 끼우면서 별첨 **'캠페인 소식 및 후원 정보 수신 동의'** 가 빠졌습니다.
같은 글이 폼 안에 따로 있어 화면은 안 깨지지만, 방침 쪽에 다시 넣을지는
아직 정하지 않았습니다.

> **수신 동의는 원문대로 '필수'로 갑니다** (2026-08-20 사용자 결정).
> 캠페인 참여에 꼭 필요하지 않은 홍보성 수신 동의를 필수로 걸면 개인정보 보호법상
> 문제가 될 수 있다는 점은 사용자가 알고 있으며, 나중에 필수/선택으로 나눌 수 있게
> `privacy.html` 안에 HTML 주석으로 근거를 남겨 두었습니다.

### 아직 예시(더미) 데이터인 것
투표 후보 5개, 예산감시 사업 4건과 그 보고서 본문, 예시 제보 2건.
사이트가 이미 밖에서 열리므로 **화면에 내지 않고 `DOK_SHOW_SAMPLE = false` 로
감춰 두었습니다**(예전에는 "예시 데이터입니다" 딱지를 붙여 보여 줬습니다).
실제 값으로 갈아 끼운 뒤 `true` 로 바꾸세요.

## 배포

**배포는 GPT/Codex 담당입니다.** Claude는 코드만 수정하고 배포하지 않습니다.

배포에 필요한 내용은 [DEPLOY.md](DEPLOY.md)에 모아 두었습니다 —
처음 한 번 세팅, 비공개로 두는 방법, 나중에 플랫폼을 옮길 때 걸리는 것,
공개 전환 전 개인정보 점검.
