# 작업 규칙 — Claude / GPT 가 먼저 읽는 파일

**사용자는 이 파일을 읽지 않는다.** 사람에게 설명하려고 쓰는 것이 아니라,
Claude 와 GPT 가 같은 판단을 이어가라고 두는 파일이다.
Claude Code 는 `CLAUDE.md`, Codex 는 `AGENTS.md` 를 폴더를 열 때 자동으로 읽는다.
**두 파일은 내용이 같다** — 한쪽을 고치면 다른 쪽도 같이 고친다.
**원칙은 여덟 줄로 고정한다 — 아홉 번째를 넣으려면 하나를 뺀다.**
여기가 길어지면 대화를 열 때마다 그만큼 값을 치른다.

## 기록 원칙 — 여섯 저장소 공통

1. **결정은 그 저장소에 적는다.** 대화로만 끝내지 않는다. 적지 않은 결정은 다음 사람에게 없는 것이다.
2. **기록은 `README.md` 한 곳에만 남긴다.** 새 문서를 만들고 싶어지면, 그때는 만들 때가 아니라 **걷어낼 때**다.
3. **지나간 것은 지운다.** "이건 지나간 얘기입니다" 딱지를 붙여 남기지 않는다. 지워도 깃 기록에 남는다.
4. **한 일보다 왜 그렇게 정했는지를 적는다.** 이유가 없으면 같은 제안이 또 올라온다.
5. **`README.md` 가 300줄을 넘으면 그날 걷어낸다.**
6. **저장소 하나에 폴더 하나.** 같은 저장소를 두 곳에 받아두지 않는다.
7. **적었으면 올린다.** 올리지 않은 기록은 나만 보는 기록이다.
8. **일은 `main` 에서 한다.** 갈래를 팠으면 끝나는 대로 합친다.

1번이 뼈대고 나머지는 그것을 지탱한다 — 2·3·5번은 적을 곳이 무거워지지 않게,
6·7·8번은 적은 것이 상대에게 닿게 한다.

이 여덟 줄은 `action-home-renewal` · `budget-mbti` · `budget-mbti-survey` ·
`participatory-budget` · `dokseong` · `fundraising-pub-page` 여섯 곳에 똑같이 있다.
고칠 일이 있으면 여섯 곳을 같이 고친다.

## 고치기 전에

- **이미 있는지 먼저 본다.** 새 파일·새 절을 만들기 전에 같은 것이 있는지 찾는다.
  없어서 못 적은 적보다, 있는 줄 모르고 또 만든 적이 훨씬 많다.
- **이 폴더가 최신인지 본다** — 아래 「이 저장소」를 볼 것.
- 사용자에게 되물을 일과 그냥 할 일을 가른다. 이미 정해진 것은 되묻지 않는다.

## 이 저장소 — `dokseong`

- **살아 있는 폴더** — `OneDrive\문서\ChatGPT\밑빠진독상 페이지\dokseong` — 클론은 하나뿐이다.
- **브랜치** — `codex/sites-share-link`. 깃허브 기본 브랜치는 `main` — 합치기 전까지 상대가 못 본다.
- 하단(footer)은 `action-home-renewal` 의 것을 읽어 온다. 하단이 바뀌면 `python tools/port-footer.py` 로 다시 굽는다.
- 캠페이너스 이식본은 GPT 가 로그인해 관리한다. Claude 는 그 화면을 직접 못 본다.
